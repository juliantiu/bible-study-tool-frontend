import { useEffect, useState } from "react";

export default function useVerseRequester(verses: string) {
  useEffect(
    () => {
      async function someFunction() {

      }

      someFunction();
    },
    []
  );
}